# 当前会话交接

## 最新状态（2026-07-24，GKI_KernelSU_SUSFS HWID 独立仓库）

### 已完成
- 从 `BailinT/GKI_KernelSU_SUSFS`（默认分支 `dev`）克隆独立测试副本：`E:\DeepSeek-Reasonix\GKI\GKI_KernelSU_SUSFS-hwid`。
- 已创建私有新仓库：`https://github.com/BailinT/GKI_KernelSU_SUSFS-hwid`（PRIVATE）。
- `origin` 指向新仓库，`upstream` 指向原仓库且 push URL 为 `DISABLED`；原仓库未改。
- 已加入全白名单 HWID 校验：`hwid/allowlist.txt` 当前只有 `abbe0d2c`。
- 注入点写在共享 `.github/workflows/build.yml`（`repo sync` 之后、Stock Config 之前），一处覆盖 a12~a16 全部版本。
- GitHub 已识别全部 workflow（5 个 kernel-aXX + 共享 build 核心）均 active；Actions 权限 enabled；默认分支已设为 `main`。
- 功能提交（快照）：`d7dd58b`。

### 实现规则
- `hwid/allowlist.txt` 所有有效行编译进同一内核；名单内开机，名单外或读不到 HWID 则 early boot panic，`panic_timeout=1` 后重启。
- 优先 bootconfig `androidboot.chipid`，回退 `oplusboot.serialno`，兼容 legacy cmdline。
- `hwid_lock.c` 不 include `<linux/panic.h>`（5.10 无此头；`<linux/kernel.h>` 已覆盖 `panic()`/`panic_timeout`）。

### ZIP 泄露检查
- 本仓库构建产物是 `AnyKernel3/* + Image + *.img`，**没有** `ak3.log` 之类把仓库地址写进包的步骤，无需删除产物。
- 仅 `get-manager.yml` 日志 echo run_id、`main.yml` release target 用 `$GITHUB_SHA`，都不进分发包。

### 推送说明（大仓库注意）
- 原仓库是 shallow clone + 含 mp4/png 二进制，完整历史（1474 提交）push 多次超时/`index-pack failed`。
- 最终改用**单提交快照**推到 `main`（无历史，pack 小，秒传）。如需完整历史，另行 unshallow 后重推。

### 已验证
- 注入器 `bash -n` 通过。
- 用真实 AOSP `android12-5.10` 与 `android16-6.12` 的 `init/main.c`、`init/Makefile` 确认锚点存在。
- 用真实 `android16-6.12` 源码跑完整注入模拟：`init/hwid_allowlist.h` 生成正确、`init/Makefile` 加入 `hwid_lock.o`、`init/main.c` 落下声明与 `hwid_lock_verify();` 调用。
- 尚未在 Actions 上完整编译，也未真机刷入验证；刷前保留原 boot 镜像与 fastboot/recovery 回滚路径。

### 下一步
- 在 Actions 手动触发某个 `kernel-aXX` 验证编译。
- 需要多设备放行时，向 `hwid/allowlist.txt` 逐行加 HWID 并重新构建。
